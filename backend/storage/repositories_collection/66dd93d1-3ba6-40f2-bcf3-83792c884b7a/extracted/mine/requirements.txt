# -------------------------------------------------------------
# Example requirements.txt
# -------------------------------------------------------------

# 1. No version constraint (always fetches the latest version)
requests

# 2. Pin to an exact version (highly recommended for production)
faker==24.1.0
numpy==1.26.4

# 3. Minimum or maximum version limits
pandas>=2.2.0
scipy<1.13.0

# 4. Compatible release (allows minor patches but prevents major breaking updates)
# Matches any version >= 2.0.0 and < 3.0.0
scikit-learn~=2.0.0 

# 5. Environment markers (only installs this package if running on Windows)
pywin32; sys_platform == 'win32'

# 6. Install directly from a specific GitHub repository branch or commit
git+https://github.com

# 7. Reference and load another requirements file (useful for separating Dev vs Prod)
-r base-requirements.txt
